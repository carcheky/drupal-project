name: Makefile CI

on:
  push:
    branches: [ "9.4.x" ]
  pull_request:
    branches: [ "9.4.x" ]

jobs:
  build:

    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v3

    - name: configure
      run: ./configure

    - name: Install dependencies
      run: make

    - name: Run check
      run: make check

    - name: Run distcheck
      run: make distcheck
